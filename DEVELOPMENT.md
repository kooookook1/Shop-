# 🛠️ بيئة التطوير — متجر ريكسون الرقمي R3XON

دليل شامل لكل أدوات التطوير والاختبار المثبّتة في هذا المستودع.

---

## 🚀 التشغيل السريع

```bash
npm install          # تثبيت كل الحزم (بما فيها أدوات التطوير)
npm run dev          # تشغيل الخادم + الواجهة على http://localhost:3000
```

الخادم ينشئ قاعدة البيانات المحلية `local.db` ويبذرها تلقائياً عند أول إقلاع
(منتجات، تصنيفات، بانرات، كوبون REX20، إعدادات). للإنتاج السحابي اضبط
`TURSO_DATABASE_URL` و `TURSO_AUTH_TOKEN` في ملف `.env`.

---

## 🧰 طبقات الأدوات

| الأداة | الغرض | الأمر |
|---|---|---|
| **TypeScript** | كشف أخطاء الأنواع | `npm run typecheck` |
| **ESLint** | كشف الأخطاء المنطقية وهُواة React | `npm run lint:eslint` |
| **Vitest** | اختبارات الوحدة (سلامة البيانات المرجعية) | `npm test` |
| **Smoke Test** | فحص 41 حالة لكل نقاط الـ API الحية | `npm run test:smoke` |
| **Playwright E2E** | رحلة المستخدم الكاملة في متصفح حقيقي | `npm run test:e2e` |
| **Screenshots** | لقطات شاشة آلية لكل صفحات التطبيق | `npm run screenshot` |
| **Doctor** | تشخيص البيئة كاملة بضغطة واحدة | `npm run doctor` |
| **Verify** | فحص سريع (أنواع + وحدات) قبل أي كوميت | `npm run verify` |

---

## 🌐 المتصفح الآلي (Headless Chromium)

الشبكة في هذه البيئة تمنع تحميل متصفحات Playwright الرسمية من CDN، لذلك
يعتمد الإعداد على حزمة **`@sparticuz/chromium`** المنزّلة عبر npm (تحتوي
المتصفح مضغوطاً داخل الحزمة نفسها — لا حاجة لأي تحميل خارجي):

- **`scripts/browser-env.mjs`** — وحدة مشتركة تستخرج:
  - ثنائية Chromium إلى `/tmp/chromium`
  - مكتبات النظام المرفقة (`al2023.tar.br`: libnss3 وإخوتها)
  - الخطوط العربية (Tajawal + Noto Naskh) إلى مجلد fontconfig
  - ثم تطلق متصفحاً جاهزاً عبر `playwright-core`

> ⚠️ علم `--single-process` محذوف عمداً من الإطلاق — مخصص لبيئة AWS Lambda
> فقط، ويسبب انهيار المتصفح بالكامل بين الاختبارات على الأنظمة العادية.

---

## 📸 لقطات الشاشة

```bash
npm run screenshot              # كل الصفحات → screenshots/
node scripts/screenshot.mjs home,cart   # مجموعة فرعية
node scripts/screenshot.mjs --out=shots/ # مجلد إخراج مخصص
```

الصفحات المتاحة: `login, home, search, product, cart, checkout, profile,
purchases, support, admin` — يسجّل السكربت حساباً تجريبياً جديداً، يشحن
محفظته بـ 500$ ويلتقط الرحلة الكاملة بدقة مضاعفة (2x) بمقاسات هاتف
430×930.

---

## 🧪 اختبارات E2E (‏`e2e/app.spec.ts`)

ثلاثة اختبارات حرجة تعمل على Chromium حقيقي بمقاسات Pixel 7:

1. **رحلة الشراء الكاملة** — تسجيل → بحث → تفاصيل منتج → شحن محفظة → سلة →
   كوبون REX20 → تأكيد الدفع → التحقق من المفتاح المُسلَّم في «مشترياتي»
   ومن الـ API. الاختبار **ينشئ منتجه الخاص** (بيانات هرمية) حتى لا يتأثر
   بنفاد مفاتيح المنتجات المزروعة.
2. **رفض كلمة المرور الخاطئة** — مع رسالة الخطأ العربية المنبثقة.
3. **التصنيفات والبانرات المزروعة** تظهر في واجهة المتجر.

```bash
npm run test:e2e                     # تشغيل الكل
npx playwright test -g "wrong"       # اختبار واحد
npx playwright show-trace test-results/.../trace.zip  # تتبع الفشل
```

---

## 🔥 اختبار الدخان للـ API (‏`scripts/smoke-test.mjs`)

41 فحصاً حياً يغطي: القراءات الأساسية، التسجيل/الدخول (بما فيه رفض
التكرار وكلمة المرور الخاطئة)، المحفظة، الطلبات بمفاتيح تلقائية (خصم
المخزون ذرّياً)، رفض الرصيد غير الكافي، الكوبونات، الرسائل، جولات
إنشاء/تعديل/حذف كاملة، وبوابة آسياسيل (فحوص آمنة دون اتصال خارجي).

---

## 🩺 فاحص البيئة (‏`scripts/dev-doctor.mjs`)

```bash
npm run doctor
```

يفحص: Node، npm، الحزم، سلامة الأنواع، الإعدادات، إطلاق المتصفح فعلياً،
دعم الخط العربي داخل المتصفح، الخادم الحي، وقاعدة البيانات المزروعة.

---

## 🗂️ بنية الأدوات

```
scripts/
  browser-env.mjs   ← إقلاع المتصفح + الخطوط + انتظار الخادم
  screenshot.mjs    ← لقطات الصفحات الآلية
  smoke-test.mjs    ← 41 فحصاً للـ API
  dev-doctor.mjs    ← تشخيص البيئة الشامل
e2e/
  global-setup.ts   ← جهوزية المتصفح والخادم قبل الاختبارات
  app.spec.ts       ← رحلات المستخدم الحرجة
tests/unit/         ← اختبارات سلامة البيانات المرجعية (vitest)
playwright.config.ts
vitest.config.ts
eslint.config.js
```

---

## 📋 تلميحات

- الخادم يزيل `local.db` من Git — هي أصل تشغيلي يُنشأ تلقائياً.
- `test-results/` و `screenshots/` مستثناة من Git.
- صور المنتجات الخارجية (Unsplash/Google) قد لا تظهر داخل البيئة المعزولة —
  هذا متوقع ولا يشير لعطل.
- حساب الأدمن الافتراضي: `zero@gmail.com` (أنشئه بكلمة مرور من شاشة التسجيل
  لتظهر لك تبويبة «لوحةالتحكم» التلقائية).
